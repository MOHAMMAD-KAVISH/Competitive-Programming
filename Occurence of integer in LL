int count(struct Node* head, int key) {
        // add your code here
        Node* curr=head;
        int c=0;
        while(curr){
            if(curr->data==key){
                c++;
            }
            curr=curr->next;
        }
        return c;
    }
